name: keep supabase awake

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: hit a public endpoint that queries the database
        run: curl -sf https://healthlens-server.vercel.app/api/health -o /dev/null
